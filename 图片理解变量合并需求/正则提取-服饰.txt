let fullText = {{chainId}}; // 替换为你的视觉理解链的输出
fullText = (typeof fullText === 'object') ? JSON.stringify(fullText) : String(fullText);

// 匹配 "clothing": "内容" 或 'clothing': '内容'
const clothingRegExp = /["']clothing["']\s*:\s*["']([^"']+)["']/;
const match = clothingRegExp.exec(fullText);
const clothing = match ? match[1].trim() : '';
clothing;