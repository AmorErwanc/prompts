let fullText = {{chainId}}; // 替换为你的视觉理解链的输出
fullText = (typeof fullText === 'object') ? JSON.stringify(fullText) : String(fullText);

// 匹配 "hairstyle": "内容" 或 'hairstyle': '内容'
const hairstyleRegExp = /["']hairstyle["']\s*:\s*["']([^"']+)["']/;
const match = hairstyleRegExp.exec(fullText);
const hairstyle = match ? match[1].trim() : '';
hairstyle;