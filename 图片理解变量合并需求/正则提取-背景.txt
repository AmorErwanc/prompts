let fullText = {{chainId}}; // 替换为你的视觉理解链的输出
fullText = (typeof fullText === 'object') ? JSON.stringify(fullText) : String(fullText);

// 匹配 "background": "内容" 或 'background': '内容'
const backgroundRegExp = /["']background["']\s*:\s*["']([^"']+)["']/;
const match = backgroundRegExp.exec(fullText);
const background = match ? match[1].trim() : '';
background;